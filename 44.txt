<!DOCTYPE html>
<html>
<head>
  <title>Admin - GocaLux</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>

<header>GocaLux Admin</header>

<section style="text-align:center;">
  <h1>All Bookings</h1>
  <div id="list"></div>
</section>

<script>
let all = JSON.parse(localStorage.getItem("bookings")) || [];

let html = "";

all.forEach((b, i) => {
  html += `
    <div class="card">
      <h3>${b.name}</h3>
      <p>${b.phone}</p>
      <p>${b.service}</p>
      <p>${b.date} - ${b.time}</p>
    </div>
  `;
});

document.getElementById("list").innerHTML = html;
</script>

</body>
</html>